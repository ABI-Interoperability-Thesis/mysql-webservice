{
    "direct": "result = input_data",
    "db-lookup": "const RunAsync = async () => {result = await DBLookup(field, input_data, model)}; RunAsync()",
    "calc-age-date": "result = calculateAge(input_data)",
    "calc-time-seconds-date": "result = CalculateDateSeconds(input_data)",
    "calc-sec-days": "result = calculateSecondsDays(input_data)"
}